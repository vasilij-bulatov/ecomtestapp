import {createTheme} from '@rneui/themed';

const theme = createTheme({
  lightColors: {
    primary: '#e34234',
    secondary: '#34d5e3',
  },
  darkColors: {
    primary: '#000',
  },
  mode: 'light',
});

export {theme};
